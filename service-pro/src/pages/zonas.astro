---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { SITE } from "@/config/site";
import { ZONES } from "@/data/zones";
import SeoJsonLd from "@/components/SeoJsonLd.astro";
import mapaAMBA from "@/assets/images/mapa-amba.jpg";
---

<BaseLayout
  title={`Zonas de atención | ${SITE.name}`}
  description={`Zonas de atención de ${SITE.name} en CABA y AMBA.`}
  pathname="/zonas"
>
  <SeoJsonLd />

  <section class="section">
    <div class="container">
      <h1 class="h1">Zonas de atención</h1>
      <p class="p">
        Atendemos <strong>{SITE.coverage}</strong>. Indicá tu barrio/partido al reservar.
      </p>

      {/* MAPA */}
      <div class="zonesMapWrap" style="margin-top:18px;">
        <img
          src={mapaAMBA.src}
          width={mapaAMBA.width}
          height={mapaAMBA.height}
          alt="Mapa de cobertura AMBA"
          loading="lazy"
          decoding="async"
        />
        <div class="zonesMapOverlay"></div>
      </div>

      <div class="divider"></div>

      {/* ZONAS (vertical + centrado) */}
      <div class="zonesCards">
        <section class="zoneCard zoneCard--caba" aria-label="Zonas CABA">
          <div class="zoneCard__head">
            <h2 class="zoneCard__title">CABA</h2>
            <span class="zoneCard__badge">{ZONES.caba.length} zonas</span>
          </div>

          <div class="zoneChips">
            {ZONES.caba.map((z) => (
              <span class="zoneChip">{z}</span>
            ))}
          </div>
        </section>

        <section class="zoneCard zoneCard--amba" aria-label="Zonas AMBA">
          <div class="zoneCard__head">
            <h2 class="zoneCard__title">AMBA</h2>
            <span class="zoneCard__badge">{ZONES.amba.length} zonas</span>
          </div>

          <div class="zoneChips">
            {ZONES.amba.map((z) => (
              <span class="zoneChip">{z}</span>
            ))}
          </div>
        </section>
      </div>

      <p class="zonesNote">
        ¿No ves tu zona? Igual escribinos: solemos cubrir cercanías según disponibilidad.
      </p>
    </div>
  </section>
</BaseLayout>


