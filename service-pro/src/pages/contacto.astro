---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { SITE } from "@/config/site";
import SeoJsonLd from "@/components/SeoJsonLd.astro";

const wa = `https://wa.me/${SITE.whatsappE164}?text=${encodeURIComponent(
  "Hola! Quiero coordinar una visita."
)}`;
---
<BaseLayout
  title={`Contacto | ${SITE.name}`}
  description={`Contactá a ${SITE.name} por WhatsApp. Atención en ${SITE.coverage}.`}
  pathname="/contacto"
>
  <SeoJsonLd />

  <section class="section">
    <div class="container">
      <h1 class="h1">Contacto</h1>
      <p class="p">La forma más rápida es por WhatsApp. Sin registros, sin vueltas.</p>

      <div class="ctaRow" style="margin-top:18px;">
        <a class="btn btn--wa btn--pro" href={wa} aria-label="Abrir WhatsApp">
          <i class="bi bi-whatsapp btnIcon"></i>
          <span class="btn__badge">WhatsApp</span>
        </a>

        <a class="btn btn--ghost btn--pro" href="/servicios">Ver servicios</a>
      </div>

      <div class="divider"></div>

      <p class="p"><strong style="color: var(--text-on-dark);">Cobertura:</strong> {SITE.coverage}</p>
      <p class="p"><strong style="color: var(--text-on-dark);">Horarios:</strong> {SITE.hours}</p>
      <p class="p"><strong style="color: var(--text-on-dark);">Tel:</strong> {SITE.phoneDisplay}</p>
    </div>
  </section>
</BaseLayout>
